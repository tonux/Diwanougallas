<%= form_for(@category_video) do |f| %>
  <% if @category_video.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@category_video.errors.count, "error") %> prohibited this category_video from being saved:</h2>

      <ul>
      <% @category_video.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nom %><br />
    <%= f.text_field :nom %>
  </div>
  <div class="field">
    <%= f.label :desc %><br />
    <%= f.text_area :desc %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
